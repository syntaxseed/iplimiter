{
    "name": "syntaxseed/iplimiter",
    "description": "A PHP IP logging library to track attempts and time since last attempt for an event by an IP address. Run an event against a ruleset to determine pass/fail. Supports banning.",
    "keywords": ["IP", "logging", "PDO", "limiting", "IP Address"],
    "license": "MIT",
    "authors": [
        {
            "name": "Sherri Wheeler",
            "homepage": "http://syntaxseed.com",
            "role": "Developer"
        }
    ],
    "type": "library",
    "require": {
		"php": ">=7.2"
    },
	"require-dev": {
		"phpunit/phpunit": "5.2.*"
    },
	"autoload": {
        "psr-4": {
            "Syntaxseed\\IPLimiter\\": "src/"
        }
    }
}
